apiVersion: v2
name: harakiri
description: Harakiri Sandbox control plane (API, web, scheduler, template builder) on top of OpenSandbox.
type: application
# version  = the chart version (SemVer). Overridden by CI from the git tag.
# appVersion = the Harakiri release the chart deploys (the image tag default).
version: 0.5.0-rc.4
appVersion: "0.5.0-rc.4"
home: https://github.com/nabilblk/h-sandbox
sources:
  - https://github.com/nabilblk/h-sandbox
keywords:
  - sandbox
  - opensandbox
  - agents
  - control-plane
maintainers:
  - name: Harakiri Sandbox maintainers
    url: https://github.com/nabilblk/h-sandbox
