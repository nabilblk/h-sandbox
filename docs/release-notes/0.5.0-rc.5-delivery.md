# 0.5.0-rc.5 Delivery

September 9, 2026. **Published; superseded for launch acceptance by rc.6.**
This receipt distinguishes source checks, publication and runtime acceptance;
it is not an announcement or a claim that unfinished gates passed.

## Changes

- Honest usage coverage and unavailable historical measurements; concurrency
  remains an unenforced target, not atomic admission.
- Runtime-aware command working directories and guarded development seeding.
- Generated Markdown, documentation indexes, preview/operator guidance,
  community policies and corrected license notices.
- Bounded dependency security updates, pinned CI actions, source/secret checks,
  trusted-main publishing, matching versions and immutable artifact guards.
- Isolated native installation configuration with independent random secrets,
  constrained OIDC clients and a realm-scoped administrative service account.

## Intended Bundle

| Artifact | Version/channel | Verification |
| --- | --- | --- |
| API, scheduler, builder image | `harakiri-api:0.5.0-rc.5` | Published, both architectures |
| Web/docs image | `harakiri-web:0.5.0-rc.5` | Published, both architectures |
| Harakiri Helm chart | `0.5.0-rc.5` | Published; anonymous download verified |
| SDK and CLI | `0.5.0-rc.5`, npm `next` at publication | Published; anonymous exact consumer install verified |
| Stable npm packages | `latest: 0.4.0` | Must remain unchanged |
| OpenSandbox chart | `0.2.2-harakiri.2` | Anonymous download and isolated Helm install passed |

Source/tag: `792ac9c5f4bd9d36ceb56ca08549ae5efaa3e50a`, `v0.5.0-rc.5`.
Harbor workflow [34346895154](https://github.com/nabilblk/h-sandbox/actions/runs/34346895154)
passed; chart OCI digest is
`sha256:ae03fce3e923db1626a26a90da668d28823c50812e6b2d62dbec71a243140ef7`.
Npm CI verification passed but publication was denied; a reviewed clean-tag local
publish succeeded. Trusted-publisher authorization remains an automation gap.

Full arm64 image scans found 221 API and 177 web package/advisory matches,
including inherited package-manager and distribution packages. These are scanner
matches, not demonstrated exploits. They prompted rc.6 security-image follow-up;
rc.5 is not declared launch-ready. See the [rc.6 receipt](0.5.0-rc.6-delivery.md).

The existing OpenCode arm64 candidate is
`core.campus.clusterdiali.me/harakiri/templates/opencode@sha256:6b300e6ecde41a6174428f13f39663462c32dd7f6799e5af863cd50245d95de5`.
Its registry metadata was read anonymously. Full layer consumption and native
workflow verification belong in the acceptance results below, not this lookup.

## Verification

Frozen dependency install, workspace build and typecheck passed. All 572 tests
passed with zero failures/skips against disposable loopback PostgreSQL after
applying migrations 001-037. An initial unmigrated-database failure is retained
in private evidence. Five release/deployment/configuration guard tests also pass.
Documentation link validation and `git diff --check` pass.

The isolated reference cluster is k0s `v1.36.3+k0s.2`, Linux/arm64, 8 CPUs,
16 GiB RAM, Ubuntu 24.04 and `local-path` storage. PostgreSQL 16.15, Keycloak
26.7.3 and the published OpenSandbox chart are running with new operator-owned
credentials. No lab identity, database, BackgroundAgent installation or default
kubeconfig was reused. An initial namespace configuration failure is retained
in private evidence; the corrected public configuration explicitly sets both
OpenSandbox subchart namespaces before installation.

Full published SDK/CLI execution, persistence, upgrade/recovery, cleanup and
the owned public lab deployment remain in progress. Follow the
[native installation instructions](../../infra/preview/README.md).

## Launch Boundary

The owner reports independent evaluation complete. No evaluator details are
invented here. Repository opening, anonymous source verification and announcement
remain a final owner decision. Stable tags are unchanged. Restricted OpenShift,
amd64 native acceptance, managed uptime and hostile multi-tenant isolation are
not certified by this arm64 developer-preview profile.
