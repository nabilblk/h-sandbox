# 0.5.0-rc.11 Delivery Receipt

Status: preparation and qualification in progress. This file is not evidence
that the packages or images have been published.

- Source review: [PR #52](https://github.com/nabilblk/h-sandbox/pull/52).
- Required CI: [35907737952](https://github.com/nabilblk/h-sandbox/actions/runs/35907737952), passed, including Node 20/22 installed consumers and documentation programs.
- Native qualification: [35907737905](https://github.com/nabilblk/h-sandbox/actions/runs/35907737905), partial; details below.
- SDK/CLI npm publication: pending, using the existing protected trusted publisher.
- Adapter npm publication: blocked on successful real-model qualification,
  first-package npm access and publisher setup;
  the manifest remains private and documentation uses explicit source tarballs.
- Harbor images/chart: pending.
- GitHub release/tag and anonymous artifact verification: pending.
- No running lab/customer deployment is changed by this delivery.

The untracked private research directory is excluded from source review,
scanning and publication. No production credentials or model keys are used for
acceptance. The runner model and sandbox have independent ownership and cleanup.

## Native Evidence and Release Scope

Run `35907737905-1` tested PR merge source
`7a16224d2946dd287bbfa405d3bcb0abd244ae6a` against published API `0.5.0-rc.9`
on September 23, 2026, 19:16-19:29 UTC. It passed 13 gates: published installation,
packed candidate, OIDC onboarding, template import, creation/finite tasks,
capacity errors, text/binary files, process reconnect/cancellation, protected
HTTP/local Git, retained workspaces/confirmed release, partial-source recovery,
unconfirmed cleanup/expiry and the actual framework shell/file/search tools.

The real-model gate used runner-local Ollama and digest-pinned Qwen3 4B. It
failed with `HarakiriRunError` at `run-repair.ts:67`, the independent final
`node --test` command after the graph invocation and unchanged-test hash check.
The sanitized evidence does not establish why that command failed. Do not
infer a successful repair or attribute a definite root cause to the model.
The later key-revocation gate was not reached in this run. Browser,
port-forward, owned runtime and private-material cleanup all passed.

Earlier run [35905669810](https://github.com/nabilblk/h-sandbox/actions/runs/35905669810)
also passed the native tool gate but failed model repair without detailed
coordinates; cleanup passed. The follow-up did not weaken outcome verification.

Core SDK/CLI publication is separate from the new, still-private adapter.
Required CI and the established SDK contracts qualify the core client update;
the source preview explicitly retains the failed real-model gate and does not
claim npm availability. No new published-pair server recovery or restricted
OpenShift qualification is inferred from these client tests.
