# 0.5.0-rc.8 Delivery

September 9, 2026. Delivery verification in progress; no announcement.

This candidate fixes the Helm-mounted Nginx documentation locations identified
during rc.7 acceptance. Markdown/JSON documentation and plain-text indexes must
have correct MIME types; missing documentation must return 404, not the SPA shell.
A source regression test covers both the standalone image and Helm configurations.
All rc.7 application, SDK and CLI behavior is retained.

The matching images, chart and npm packages will have immutable versioned receipts
below after verification. Stable npm latest stays 0.4.0; source visibility remains
private. Independent evaluation is owner-confirmed complete. Point 5 remains the
separately approved final decision, not an action performed by this release.
